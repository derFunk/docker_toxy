# docker_toxy
Docker container for Toxy proxy from https://github.com/h2non/toxy

## About
Toxy proxy listens on port 3000.

All requests are forwarded to a local instance of tinyproxy, to forward _all_ requests to the internet.
